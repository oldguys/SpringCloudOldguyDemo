server:
#  port: 9014
  port: 0
spring:
  application:
    name: oldguy-log-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  mvc:
    date-format: yyyy-MM-dd HH:mm:ss
logging:
  level:
    com.example.oldguy: debug
#    root: debug